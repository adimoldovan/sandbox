name: Steps test

on:
  push:

jobs:
  multiple-commands:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run some bash commands
        run: |
          bash ./.github/workflows/fail.sh || true
          echo "I ran after a failed commad"
