name: Paths tests

on:
  push:
    branches: [main]
    paths:
      - 'src/folder_one/**'
  pull_request:
    paths:
      - 'src/folder_three/**'

jobs:
  test-paths-job:
    name: "folder_one changed"
    runs-on: ubuntu-latest
        
    steps:
    - uses: actions/checkout@v2
    
    - name: Echo stuff
      run: echo 'folder_one changed'
  
