name: Test
on:
  pull_request:
  push:
    branches:
      - main

jobs:
  run-tests:
    name: "Run tests"
    runs-on: ubuntu-latest
    strategy:
      matrix:
        version: [10, 12, 14]

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run tests
        run: echo "running tests"
