name: Test pm2

on:
  push:
    branches: [master]
    paths-ignore:
    - '**.md'

jobs:
  test-pm2:
    - name: Install and run pm2
      run: |
        yarn install
        yarn pm2-tunnel
        cat /home/runner/.pm2/pm2.log

    - name: Test
      run: cat tunnel-url
